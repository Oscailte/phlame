<?php use Phalcon\Tag; ?>

<?php echo $this->getContent(); ?>

<table width="100%">
    <tr>
        <td align="left">
            <?php echo $this->tag->linkTo(array("types/index", "Go Back")); ?>
        </td>
        <td align="right">
            <?php echo $this->tag->linkTo(array("types/new", "Create ")); ?>
        </td>
    </tr>
</table>

<table class="browse" align="center">
    <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
         </tr>
    </thead>
    <tbody>
    <?php foreach ($page->items as $type) { ?>
        <tr>
            <td><?php echo $type->id ?></td>
            <td><?php echo $type->name ?></td>
            <td><?php echo $this->tag->linkTo(array("types/edit/" . $type->id, "Edit")); ?></td>
            <td><?php echo $this->tag->linkTo(array("types/delete/" . $type->id, "Delete")); ?></td>
        </tr>
    <?php } ?>
    </tbody>
    <tbody>
        <tr>
            <td colspan="2" align="right">
                <table align="center">
                    <tr>
                        <td><?php echo $this->tag->linkTo("types/search", "First") ?></td>
                        <td><?php echo $this->tag->linkTo("types/search?page=" . $page->before, "Previous") ?></td>
                        <td><?php echo $this->tag->linkTo("types/search?page=" . $page->next, "Next") ?></td>
                        <td><?php echo $this->tag->linkTo("types/search?page=" . $page->last, "Last") ?></td>
                        <td><?php echo $page->current, "/", $page->total_pages ?></td>
                    </tr>
                </table>
            </td>
        </tr>
    </tbody>
</table>
